server:
  port: 8085

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:oracle:thin:@//host.docker.internal:1521/xe
    driver-class-name: oracle.jdbc.OracleDriver
    username: scott
    password: tiger

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq        # ✅ FIXED
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/   # ✅ FIXED

  instance:
    prefer-ip-address: true