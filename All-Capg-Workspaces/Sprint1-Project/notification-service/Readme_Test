1. testGetAllNotifications()
✅ What it does:

👉 Checks if all notifications are fetched correctly

🔍 Internally:
Mock DB → returns 1 notification
Service → converts it to response
Test → checks list size
🎤 Say in viva:

“This test verifies that the service correctly retrieves and maps all notifications.”

🧪 2. testGetNotificationById_Success()
✅ What it does:

👉 Checks valid ID case

🔍 Internally:
Mock DB → returns notification
Service → returns mapped response
Test → checks data is not null
🎤 Say:

“This test ensures correct data is returned when a valid ID is provided.”

🧪 3. testGetNotificationById_NotFound()
❌ What it does:

👉 Checks invalid ID (failure case)

🔍 Internally:
Mock DB → returns empty
Service → throws exception
Test → verifies exception
🎤 Say:

“This test validates error handling when the notification is not found.”

🧪 4. testGetNotificationsByUserId()
✅ What it does:

👉 Checks user-specific notifications

🔍 Internally:
Mock DB → returns notifications for user
Service → maps them
Test → verifies userId
🎤 Say:

“This test checks if notifications are correctly filtered by user ID.”

🧪 5. testConsumePaymentEvent() (Listener Test 🔥)
✅ What it does:

👉 Tests event-based notification creation (RabbitMQ)

🔍 Internally:
Simulates payment event
Listener receives event
Creates notification
Saves to DB (mocked)
🎤 Say:

“This test verifies that a notification is created and saved when a payment event is received.”